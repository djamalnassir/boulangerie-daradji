{% extends 'home.html.twig' %}

{% block containtFluid %}

  <div class="row">
    <div class="col-md-12">
      <div class="card">
        <!-- card-header -->
        <div class="card-header">
          <h3 class="card-title">Tableau des productions</h3>
          <div class="btn-group float-right">
              
              <a class="btn btn-block bg-gradient-primary" href="{{ path('add_detail_production') }}">
                <i class="fas fa-plus"></i>
                Ajouter
              </a>
          </div>
        </div>
        <!-- /.card-header -->

        <!-- card-body -->
        <div class="card-body">
          
            <table class="table table-bordered">
              <thead>
                <tr>
                  <th>ID Production</th>
                  <th>Matiere Premiere Prod</th>
                  <th>Quantite Matiere Premiere Prod</th>
                  <th>Date Production</th>
                  <th>Action</th>
                </tr>
              </thead>
              
                <tbody>
                  {% if detail_productions != null %}
                    {% for detail in detail_productions %}
                      <tr>
                        <td>{{ detail.production.id }}</td>
                        <td>{{ detail.matierePremiere.nom }}</td>
                        <td>{{ detail.quantite }}</td>
                        <td>{{ detail.production.dateProduction|date('d-m-Y') }}</td>
                        <td>
                          <a class="btn btn-info btn-sm" href="#">
                            <i class="fas fa-pencil-alt"></i>
                          </a>
                          <a class="btn btn-danger btn-sm" href="#">
                            <i class="fas fa-trash"></i>
                          </a>
                        </td>
                      </tr>
                    {% endfor %}
                  {% endif %}
                </tbody>
              
            </table>
        </div>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->
    </div>
    <!-- /.col -->
  </div>
  <!-- card -->

{% endblock %}